# TODO

- [x] Externalize inline CSS from `index.html` into a dedicated stylesheet.
- [x] Move JavaScript logic out of `index.html` into separate modules.
- [x] Load lyrics and chords from `data/lyrics.json` and `data/chords.json` at runtime.
- [x] Ensure local-only overwrites for JSON persistence within the installed PWA context.
- [x] Clean up remaining inline markup after dynamic rendering is in place.
